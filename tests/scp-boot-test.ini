; A simple boot test. This displays the emulator specific help function and then exits.
; This acts as a simple test to verify the build has worked correctly, but doesn't test emulator specific features.
echof "\n*** TEST PASSED ***\n"
exit 0